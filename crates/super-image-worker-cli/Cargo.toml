[package]
name = "super-image-worker"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
super-image-worker-core = { path = "../super-image-worker-core" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
rayon = "1"
